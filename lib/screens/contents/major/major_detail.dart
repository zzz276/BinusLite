import 'package:flutter/material.dart';

class MajorDetail extends StatelessWidget {
  const MajorDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(16.0), 
      child: null,
    );
  }
}
